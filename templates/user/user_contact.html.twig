{% extends 'base.html.twig' %}

{% block title %} Contacter {{user.firstName}} {% endblock %}

{% block body %} 

    <div class="container m-5 p-4">

    <h2 class="mb-2 mt-2 text-center">Ecrivez à {{user.firstName}}</h2>
        <div class="container m-2 p-4">
            {{ form_start(form) }}

                <div class="row">
                    <div class="col">{{ form_row(form.name) }}</div>
                    <div class="col"> {{ form_row(form.country) }}</div>                  
                </div>
                
                {{ form_row(form.email) }}
                {{ form_row(form.message) }}

                <button type="submit" class="btn btn-blue float-right">Envoyer</button>


            {{ form_end(form)}}
        </div>
    </div>

{% endblock %}